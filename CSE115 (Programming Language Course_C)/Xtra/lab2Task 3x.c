#include <stdio.h>

int main()
{
    char a,b,c;
    printf("Select a number between a and b:\n");
    scanf("%c",&c);

    switch(c)
    {
        case : printf("You have selected the number a\n");
        break;
        case b: printf("you have selected the number b\n");
        break;
        default: printf("The number you selected is invalid.\n");
}

    return 0;
}
